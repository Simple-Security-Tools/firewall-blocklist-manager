.idea/
.venv/
.DS_Store
__pycache__/
.pytest_cache/

# Runtime state and output — never commit
backups/
data/
# pre-rename location; kept so an old database is never committed
database/
logs/
reports/
rules/
palo-blocks.txt
palo-allows.txt

# Local secrets and site-specific config (see the .example files)
config.txt
whitelist.txt
